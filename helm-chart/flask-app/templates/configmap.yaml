apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "flask-app.fullname" . }}-nginx-config
  labels:
    {{- include "flask-app.labels" . | nindent 4 }}
data:
  nginx.conf: {{ .Values.nginx.config | nindent 4 }}
---

